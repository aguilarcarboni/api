Main database URL
{
    "title": "Database",
    "path": "main",
    "description": "The database default path.",
    "type": "object",
    "properties": {
        "profiles": {
            "type": "string",
            "path":"profiles",
            "description": "Profiles table of the databse"
        },
        "events": {
            "type": "string",
            "path":"events",
            "description": "Profiles table of the databse"
        },
        "required": [
            "profiles"
        ]
    }
}

Profiles table
{
    "title": "Profile",
    "path": "profiles",
    "description": "A profile of a person, animal or object saved in my database.",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Name of the person or object with the profile"
        },
        "full_name": {
            "type": "string",
            "description": "Full name of the person or object with the profile"
        },
        "status": {
            "type": "string",
            "description": "Type of profile (person, object, etc)."
        },
        "required": [
            "name",
            "full_name",
            "status"
        ]
    }
}

Events table
{
    "title": "Event",
    "description": "An event in my calendar.",
    "path": "events",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Name of the person or object with the profile"
        },
        "datetime": {
            "type": "string",
            "description": "Date and time the event will take place."
        },
        "required": [
            "datetime",
            "name",
        ]
    }
}